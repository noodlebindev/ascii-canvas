# ascii-canvas Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `ascii-canvas` skill — a markdown-only superpowers-style skill that composes ASCII/Unicode text-based visual artifacts (16 formats, 8 narrative arcs) with strict layout discipline and predictable output behavior.

**Architecture:** No code. The skill is a tree of markdown files. `SKILL.md` is the entry point (triggers, routing, output rules). `composition-rules/` holds shared rules (box-drawing, widths, arcs, QA). `formats/` holds 16 per-format templates. `examples/` holds 16 reference artifacts. All output is generated by Claude reading these files at use time.

**Tech Stack:** Markdown only. Git for version control. No build step, no test runner, no external CLI dependencies.

**Spec reference:** `docs/2026-05-17-ascii-canvas-design.md`

**Verification model:** Batched end-to-end acceptance suite (7 prompts) at the end. No per-file test code — format files only matter when SKILL.md routes to them.

---

## Conventions used in this plan

- All paths absolute under ``.
- Every task ends with a git commit. Use [conventional commits](https://www.conventionalcommits.org/) style: `feat:`, `chore:`, `docs:`.
- Authoring steps include the full required content for foundational files (SKILL.md, composition-rules/). For the 16 format files and 16 example files, a shared template is defined once (Tasks 7 and 23) and each per-format task supplies the specific values that fill the template.
- When a step says "verify the file contains X", re-read the file you just wrote and confirm. Do NOT skip this — it's the substitute for unit tests.

---

# Phase 0 — Repo setup

## Task 1: Initialize git repo + create folder structure

**Files:**
- Create: `.gitignore`
- Create: directory tree

- [ ] **Step 1: Create the directory tree**

Run:
```bash
mkdir -p {formats,composition-rules,examples,docs}
```

- [ ] **Step 2: Initialize git repo**

Run:
```bash
cd ~/.claude/skills/ascii-canvas && git init -b main
```

Expected output: `Initialized empty Git repository in .git/`

- [ ] **Step 3: Create .gitignore**

Write `.gitignore`:

```
.DS_Store
*.swp
*.tmp
.idea/
.vscode/
```

- [ ] **Step 4: First commit (the existing design spec is already on disk)**

Run:
```bash
cd ~/.claude/skills/ascii-canvas && \
  git add .gitignore docs/2026-05-17-ascii-canvas-design.md docs/2026-05-17-ascii-canvas-implementation-plan.md && \
  git commit -m "chore: initialize ascii-canvas skill repo with design spec and plan"
```

Expected: one commit on `main` containing 3 files.

---

# Phase 1 — Composition rules (foundation; every format depends on these)

## Task 2: Write composition-rules/box-drawing.md

**Files:**
- Create: `composition-rules/box-drawing.md`

- [ ] **Step 1: Author the file**

Full content:

````markdown
# Box-drawing rules

The skill renders in **two modes**: Unicode (default) and pure ASCII (fallback). Both must produce layout-equivalent output.

## Character inventory

### Unicode (default)

| Class | Characters |
|---|---|
| Light box | `┌` `┐` `└` `┘` `─` `│` `├` `┤` `┬` `┴` `┼` |
| Heavy box | `┏` `┓` `┗` `┛` `━` `┃` `┣` `┫` `┳` `┻` `╋` |
| Double box | `╔` `╗` `╚` `╝` `═` `║` `╠` `╣` `╦` `╩` `╬` |
| Shapes | `◆` `▲` `●` `◇` `◯` `▼` `■` `□` `◉` `◎` |
| Arrows | `→` `←` `↑` `↓` `↔` `↕` `⇒` `⇐` `⇑` `⇓` |
| Misc | `•` `·` `◦` `※` `…` |

### ASCII fallback

| Purpose | Unicode | ASCII |
|---|---|---|
| Horizontal line | `─` | `-` |
| Vertical line | `│` | `\|` |
| Corner | `┌┐└┘` | `+` |
| T-junction | `├┤┬┴┼` | `+` |
| Arrow right | `→` | `->` |
| Arrow left | `←` | `<-` |
| Arrow down | `↓` | `v` |
| Arrow up | `↑` | `^` |
| Bullet | `•` | `*` |
| Diamond | `◆` | `<>` or `*` |

## Rules

### R1 — When to use ASCII fallback

Use pure ASCII when the user says:
- "plain ASCII"
- "email-safe"
- "copy-paste safe"
- "no special characters"

Also use pure ASCII when the destination is:
- Plain-text email body
- Code comments inside source files
- Logs / stdout in CI contexts
- Old terminals or constrained environments

In all other cases: Unicode default.

### R2 — Equivalence rule

The same layout MUST render in both modes without breaking alignment. When authoring a format file, provide BOTH a Unicode skeleton and an ASCII skeleton if they differ.

If a layout works in Unicode but breaks in ASCII (e.g. uses single-cell shapes that have no good ASCII equivalent), simplify the layout — don't break the equivalence.

### R3 — Consistency rule (line weights)

Do not mix Unicode line weights in a single artifact. Specifically:
- Don't mix `─` (light) with `═` (double) or `━` (heavy)
- Don't mix `│` (light) with `║` (double) or `┃` (heavy)

The only exception: when line weight carries semantic meaning (e.g. heavy outer frame to demarcate a "container" with light cells inside). Apply consistently — every outer frame uses the same weight.

### R4 — Arrow consistency

Within one artifact:
- All "forward" arrows use the same glyph (`→` everywhere, not `→` mixed with `>`)
- All "down" arrows use the same glyph (`↓` everywhere, not mixed with `v`)
- Arrow direction must match flow direction — never use `→` for a backward reference

### R5 — Closed boxes only

Every box must close. No dangling line segments. If a join would create an unclosed shape, restructure the layout.

### R6 — Bullet style consistency

Within one artifact, pick one bullet style and use it throughout:
- `•` (Unicode default)
- `*` (ASCII fallback)
- `-` (Markdown lists in saved files)

Do not mix.
````

- [ ] **Step 2: Verify**

Re-read the file. Confirm: R1-R6 are present, character inventory is complete for both modes, all rules are stated as imperatives.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add composition-rules/box-drawing.md && \
  git commit -m "feat: add box-drawing composition rules"
```

---

## Task 3: Write composition-rules/width-budgets.md

**Files:**
- Create: `composition-rules/width-budgets.md`

- [ ] **Step 1: Author the file**

Full content:

````markdown
# Width budgets

Every format has a defined max column width. Never exceed it.

## Per-format budgets

| Format | Max width (cols) |
|---|---|
| slideshow | 80 |
| step-by-step-guide | 80 |
| learning-ladder | 80 |
| playbook | 80 |
| cheat-sheet | 90–100 |
| comparison-table | 90–100 |
| flowchart | 100 |
| timeline | 100 |
| mind-map | 100 |
| infographic | 100 |
| decision-tree | 100 |
| content-map | 100 |
| concept-stack | 100 |
| architecture-diagram | 110–120 |
| sequence-diagram | 110–120 |
| dashboard | 120 |

## Hard rules

- **Never exceed the format's max width.** This is non-negotiable.
- **Prefer shrinking content over wrapping awkwardly.** A wrapped cell is worse than a shorter label.
- **Maintain alignment over verbosity.** If a column drifts to make text fit, drop characters from the text instead.

## Overflow handling (priority order)

If the planned content exceeds the format's max width:

1. **Shorten text.** Truncate labels, use abbreviations, drop articles ("the", "a").
2. **Restructure.** Break wide rows into stacked rows. Move labels outside boxes.
3. **Split into more panels.** Two narrow panels beat one wide panel that exceeds the budget.
4. **Never** widen past the max. Never.

## Width counting

- Count **printed columns**, not raw character bytes.
- Unicode box-drawing chars are single-column (1 col wide each).
- Some emoji are double-column — avoid emoji inside framed cells.
- Tabs are forbidden (rendering width varies by terminal). Use spaces.

## Per-line check

When QA runs (per `qa-checklist.md`), verify each line in the artifact:
```
max(len(line) for line in artifact) <= format_max_width
```
````

- [ ] **Step 2: Verify**

Re-read the file. Confirm: all 16 formats appear in the table, hard rules + overflow priority order + width counting + per-line check are present.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add composition-rules/width-budgets.md && \
  git commit -m "feat: add width-budget composition rules"
```

---

## Task 4: Write composition-rules/narrative-arcs.md

**Files:**
- Create: `composition-rules/narrative-arcs.md`

- [ ] **Step 1: Author the file**

Full content:

````markdown
# Narrative arcs (multi-panel formats)

Every multi-panel format has a canonical arc — a fixed sequence of panel purposes. The format file references its arc by name.

## The 8 arcs

### A1 — Slideshow arc (5–7 panels)

1. Hook / framing
2. Context / why it matters
3. Core idea
4. Example #1
5. Example #2
6. Key takeaway
7. *Optional:* recap or next step

**Use for:** teaching concepts, content posts, explainers.

### A2 — Playbook arc (5–8 panels)

1. Objective
2. When to use / context
3. Prerequisites
4. Step 1 of execution
5. Step 2 of execution
6. Step N of execution
7. Success criteria
8. Failure modes / pitfalls

**Use for:** workflows, SOPs, AI agent systems, marketing execution.

### A3 — Learning-ladder arc (4–6 levels)

1. Foundations
2. Core mechanics
3. Practical application
4. Intermediate depth
5. Advanced nuance
6. Expert / edge cases

**Use for:** skill progression, education.

### A4 — Infographic arc (4–6 blocks)

1. Headline / key claim
2. Supporting insight #1
3. Supporting insight #2
4. Supporting insight #3
5. Synthesis / takeaway
6. *Optional:* CTA / action

**Use for:** high-signal summaries, social content.

### A5 — Concept-stack arc (4–6 layers)

1. Base layer (foundation)
2. Supporting layer
3. Supporting layer
4. Supporting layer
5. Top-level outcome

**Use for:** systems thinking, dependencies, architecture of ideas.

### A6 — Content-map arc (3–5 clusters)

1. Core idea
2. Angle cluster #1 (with hooks/posts/threads underneath)
3. Angle cluster #2
4. Angle cluster #3
5. *Optional:* distribution notes

**Use for:** content expansion, social idea trees.

### A7 — Dashboard arc (4–8 panels)

1. Title / context
2. Key metric block
3. Key metric block
4. Key metric block
5. Summary / signal

**Use for:** status views, performance summaries.

### A8 — Step-by-step-guide arc (4–7 steps)

1. Goal
2. Step 1
3. Step 2
4. Step 3
5. Outcome / verification

**Use for:** simple processes (lighter than playbook).

## Universal panel rules (apply to every arc)

### U1 — Every panel must earn its existence

If two panels say similar things → merge. If a panel has weak content → drop.

### U2 — Transition rule

Panel N must logically create the need for Panel N+1. If not, structure is wrong — reorder or rewrite.

### U3 — Default panel count

4–7 panels. Never exceed 9 without explicit user request. **Compress before expanding.**

### U4 — Content-driven reduction

If content doesn't justify the full arc → reduce panel count. **Do NOT introduce filler** to meet default ranges.

### U5 — User override

If user specifies structure ("3 slides", "5 steps") → follow user verbatim. Override default arc length.

### U6 — Format ≠ layout

Each format is a **way of organizing thinking**, not just visual furniture:
- Slideshow = narrative
- Playbook = execution
- Learning-ladder = progression
- Infographic = compression
- Concept-stack = dependency
- Content-map = expansion
- Dashboard = surveillance
- Step-by-step-guide = sequence

If the chosen format doesn't match the topic's *thinking shape*, switch formats.
````

- [ ] **Step 2: Verify**

Re-read. Confirm: 8 arcs present (A1-A8), 6 universal rules (U1-U6), all match the spec §5.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add composition-rules/narrative-arcs.md && \
  git commit -m "feat: add narrative-arc composition rules"
```

---

## Task 5: Write composition-rules/qa-checklist.md

**Files:**
- Create: `composition-rules/qa-checklist.md`

- [ ] **Step 1: Author the file**

Full content:

````markdown
# QA checklist

Two modes: **light** (single in-chat diagrams) and **strict** (saved/multi-panel/reusable/publishable artifacts).

## Light QA

Quick visual sanity check. Look at the rendered output and confirm:
- Alignment looks right
- Width fits the format budget
- Boxes appear closed

That's it. Don't over-engineer.

## Strict QA — 8-point checklist

Run before BOTH chat render AND file write. Do not write a file that has not passed strict QA.

1. **Every box closes.** No dangling line segments. No unclosed `┌─┐` without a matching `└─┘`.
2. **Every column aligns.** Vertical lines (`│`) line up across rows. Cell widths match within a column.
3. **No line exceeds the format width budget.** Verify `max(len(line)) <= format_max_width`.
4. **No awkward mid-cell wrapping.** Text inside a cell does not break across rendered lines.
5. **Arrows/connectors are readable.** Directions match flow. No ambiguous joins (where two arrows could be read as one).
6. **Unicode and ASCII fallback structures stay layout-equivalent.** If the user requested ASCII, verify the ASCII version preserves the same row/column structure.
7. **Section spacing is consistent.** All major separators identical (`---` or `===`, not mixed). All minor spacings (blank lines between sections) match.
8. **Markdown code fences preserve formatting.** When wrapped in ` ``` ` blocks, no line is broken or auto-formatted by markdown.

## Cross-artifact consistency check (in addition to the 8 points)

Verify repeated structural elements stay consistent across the WHOLE artifact:
- All boxes use the same line weight (per box-drawing.md R3)
- All panels in a multi-panel artifact have **identical width**
- All section dividers identical (no `---` and `===` mixed)
- All bullet styles match (no `•` and `-` mixed)
- All arrows pointing the same direction use the same glyph

This catches drift that point-checks on individual sections miss.

## When QA runs

- **Light QA:** every single in-chat artifact.
- **Strict QA:** every saved artifact, every multi-panel artifact, anything explicitly tagged "publish" or "reusable".
- **Strict QA runs twice:**
  1. Before rendering to chat
  2. Before writing to file
- **Pre-write gate:** the file write step must not execute unless strict QA has passed. If QA fails between render and write, regenerate, do not save the failing version.

## Failure behavior

- **Silently fix issues.** Do not surface minor corrections to the user.
- **Only warn the user** if the requested layout cannot fit cleanly at the chosen width — e.g., "Note: this is tight at 80 cols. Want me to widen to 100?"

## Fail-twice → simplify rule

If layout fails strict QA twice in a row:
- Reduce text
- Split into more panels
- Switch to a narrower/simpler format (see fallback map below)
- Move labels outside boxes
- Never force dense text into tight boxes

## Format fallback map (graceful degradation)

When the simplify rule cannot save the layout, fall back to a simpler adjacent format:

| Original | Fallback |
|---|---|
| playbook | step-by-step-guide |
| slideshow | infographic |
| infographic | cheat-sheet |
| dashboard | cheat-sheet |
| architecture-diagram | concept-stack |
| sequence-diagram | step-by-step-guide |
| mind-map | content-map |
| concept-stack | comparison-table |
| learning-ladder | step-by-step-guide |
| content-map | infographic |
| flowchart | step-by-step-guide |
| decision-tree | comparison-table |

When fallback fires, emit a one-line note:
```
Note: simplified from <original> to <fallback> for readability.
```

## No partial save rule

NEVER save:
- A preview render (chat preview shows first 1–2 sections; the file write must contain the FULL artifact)
- An artifact that failed strict QA
- An in-progress retry during the fail-twice cycle
- An interrupted generation

The pre-write gate: full artifact assembled + strict QA passed → then and only then write.

## Readability override

If strict QA passes but the artifact is still hard to read (dense, overcrowded, visually noisy):
- Simplify anyway
- A pass on the checklist is necessary but not sufficient — readability is the real goal

## General layout principles (enforced as part of QA)

### L1 — Label placement

If text does not fit inside a box at the format's width budget:
- Place the label outside the box, with a leader line (e.g. `── label`)
- Never force-cram text past box boundaries
- Never wrap inside a cell

### L2 — Visual hierarchy

- Primary content goes top or left
- Indentation signals dependency / subordination
- Proximity (no gap) + shared border = relationship
- Negative space (a blank line) signals "this is a new group"

When the artifact's hierarchy doesn't match the topic's hierarchy → restructure.

### L3 — Density control

- Default to compact layouts. Minimize whitespace.
- Only increase spacing when readability genuinely requires it (e.g. dashboard metric blocks need breathing room; a cheat-sheet does not)
- If the artifact feels sparse → tighten before shipping
- If the artifact feels claustrophobic → simplify (drop content) before widening spacing
````

- [ ] **Step 2: Verify**

Re-read. Confirm: light + strict modes, 8-point checklist, cross-artifact consistency check, QA-runs-twice rule, no-partial-save, fallback map, readability override, and the three general layout principles (L1 label placement, L2 visual hierarchy, L3 density control).

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add composition-rules/qa-checklist.md && \
  git commit -m "feat: add QA checklist composition rules"
```

---

# Phase 2 — SKILL.md (entry point)

## Task 6: Write SKILL.md

**Files:**
- Create: `SKILL.md`

- [ ] **Step 1: Author the file**

Full content:

````markdown
---
name: ascii-canvas
description: Compose ASCII/Unicode diagrams, slideshows, infographics, playbooks, and other text-based visual artifacts from a topic, idea, or set of facts. Use when the user explicitly requests a diagram/visual/format (flowchart, slideshow, cheat sheet, infographic, timeline, etc.) OR asks for a structured breakdown / step-by-step explanation of a non-trivial topic. Do NOT trigger for simple factual questions, short answers (less than ~3 steps or one paragraph), or conversational/opinion queries.
---

# ascii-canvas

Compose high-quality ASCII/Unicode text-based visual artifacts. "Canva for ASCII."

## When to fire

**Fire on:**
- Explicit format requests: "make a flowchart", "draw the architecture", "ASCII slideshow", "infographic on X", "comparison table", "timeline of", "cheat sheet for"
- Visual framing: "visualize X", "show me how X works", "diagram this", "illustrate"
- Structured breakdown of non-trivial topics: "break down X", "walk me through X step by step", "explain X as a playbook", "learning ladder for X", "content map for X"

**Do NOT fire on:**
- Simple factual questions ("what year did X happen?")
- Short answers (less than ~3 steps or one paragraph)
- Conversational replies, opinions, hot takes
- When trigger is weak AND structure won't improve clarity over prose

## Routing flow

```
1. DETECT TRIGGER
   ├── Explicit format keyword?        → load that format file
   ├── Visual framing?                 → infer format from topic shape (matrix below)
   ├── Structured breakdown of
   │   non-trivial topic?              → infer multi-panel format
   └── None / weak / won't help        → DO NOT activate; respond as plain prose

2. CHOOSE FORMAT
   If user named one → use it.
   Otherwise apply the routing matrix:

   branching + decisions         → decision-tree
   branching + mixed steps       → flowchart
   linear process                → step-by-step-guide
   repeatable workflow / SOP     → playbook
   skill progression             → learning-ladder
   one idea → many outputs       → content-map
   layers / dependencies         → concept-stack
   side-by-side options          → comparison-table
   actors + messages             → sequence-diagram
   system components             → architecture-diagram
   events over time              → timeline
   central idea + radial         → mind-map
   dense reference               → cheat-sheet
   status / metrics              → dashboard
   narrative explainer           → slideshow
   high-signal summary           → infographic

   Tie-breakers:
   - Multiple formats apply  → choose simplest that preserves clarity
   - Two formats equally fit → choose the simpler one
   - Do NOT combine formats in v1 unless explicitly requested

3. LOAD FORMAT FILE (MANDATORY)
   → Read formats/<format>.md
   No ad-hoc layouts. Templates only.

4. DETERMINE CHARACTER SET
   Unicode by default. ASCII fallback if user requested OR destination demands.
   See composition-rules/box-drawing.md.

5. APPLY CANONICAL ARC (multi-panel formats only)
   See composition-rules/narrative-arcs.md.
   Reduce panel count if content doesn't justify the full arc.

6. GENERATE OUTPUT

7. DETERMINE SAVE BEHAVIOR
   - Single-artifact + not multi-section + not "reusable asset" → chat only
   - Multi-panel / multi-section / clearly reusable → auto-save
   - User signaled iteration ("try again", "draft") → chat only
   - User said "save this" → force save
   - User said "don't save" / "chat only" → force chat

8. RUN QA
   - Strict mode for saved/multi-panel/reusable
   - Light mode otherwise
   - Strict QA must pass BEFORE chat render AND BEFORE file write
   - If fails twice → invoke simplify rule
   - If simplify still fails → invoke format fallback (see qa-checklist.md)

9. RENDER + (optional) SAVE
   - Chat preview: ≤80 lines → render full. >80 lines → render first 1-2 sections + "saved to <path>"
   - File write: only AFTER strict QA passes. Path: ~/Ascii-diagrams/YYYY-MM/YYYY-MM-DD-<slug>.md
   - Post-save line: exactly two lines, no fluff:
       Saved to:
       ~/Ascii-diagrams/YYYY-MM/YYYY-MM-DD-<slug>.md
```

## Output behavior

### Save path & naming

- Path: `~/Ascii-diagrams/YYYY-MM/YYYY-MM-DD-<topic-slug>.md`
- Slug: lowercase, hyphen-separated, 6–8 words max, derived from user intent
- Fallback slug if user intent is unclear: `<format>-<short-topic>` (e.g., `playbook-cold-outbound`)

### File template

```markdown
# Title

Short description (1–2 lines)

---

## Section 1

<ASCII inside a code block>

---

## Section 2

<ASCII>
```

### Chat preview rules

- ≤80 lines → render the full artifact inline
- >80 lines → render the first 1–2 sections + emit:
  ```
  Full artifact saved to:
  ~/Ascii-diagrams/2026-05/2026-05-17-<slug>.md
  ```
- Offer "expand to show full" only if the user might want it inline

### Anti-pattern

Do NOT default to saving everything "just in case." Throwaway sketches do not deserve files. Noise > occasional regret.

## Composition rules (read on demand)

- `composition-rules/box-drawing.md` — Unicode set, ASCII fallback, consistency rules
- `composition-rules/width-budgets.md` — per-format max width + overflow handling
- `composition-rules/narrative-arcs.md` — 8 arcs + universal panel rules
- `composition-rules/qa-checklist.md` — light/strict QA + simplify + fallback + no-partial-save

## Examples (read on demand)

`examples/<format>-example.md` — one richer reference per format. Read when the format file's inline example isn't enough.

## v1 boundaries (explicitly out of scope)

- `mode: quick | publish` flag → v2
- External tools (graph-easy, ditaa, figlet) → manual workflow only, not integrated
- Color / ANSI escapes → not used
- Image export → not in scope
- Format combination (e.g. slideshow with embedded flowchart) → only when explicitly requested

## Hard rules (do not violate)

1. **Always load the relevant format file before rendering output.** No ad-hoc layouts.
2. **Strict QA must pass before any file write.** No partial saves. No QA-failed saves.
3. **Never exceed format max width.** Shrink content; never widen past budget.
4. **Default to saving NOTHING.** Save only what matches the save criteria.
5. **Format choice: simplest that preserves clarity.** No combining formats in v1 unless asked.
````

- [ ] **Step 2: Verify**

Re-read. Confirm:
- YAML frontmatter at top with `name` and `description`
- All 9 routing steps present
- All 16 routing-matrix entries present
- Output behavior + save path + naming + file template + preview rules
- All 4 composition-rules files referenced
- 5 hard rules at the bottom

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add SKILL.md && \
  git commit -m "feat: add SKILL.md entry point with triggers and routing"
```

---

# Phase 3 — Format files (16)

## Task 7: Author the format-file template (reference for Tasks 8-22)

This task creates no file. It defines the **shared template** every format file follows. Apply it in Tasks 8–22.

**Every format file (`formats/<name>.md`) MUST contain these sections in order:**

````markdown
# <format-name>

## Purpose

One sentence describing what this format is for.

## Use when

- Bullet 1 (specific topic shapes that fit)
- Bullet 2
- Bullet 3

## Do NOT use when

- Bullet 1 (when a different format is a better fit, with the fallback named)
- Bullet 2

## Width budget

<N> cols (from composition-rules/width-budgets.md)

## Arc (if multi-panel)

References `narrative-arcs.md` section A<n>. Single-artifact formats omit this section.

## Layout rules

- Specific to this format. 3–6 bullets.

## Canonical skeleton — Unicode

```
<paste skeleton here>
```

## Canonical skeleton — ASCII fallback

```
<paste ASCII version here, only if it differs structurally from Unicode>
```

## Short rendered example

```
<a small, concrete example using a real topic>
```

## Failure modes

- "If X happens → simplify by Y"
- "If width overflows → fall back to <format> per qa-checklist.md"
````

For each of Tasks 8–22, you'll fill in the bracketed values per-format. The skeleton and example must each pass strict QA at the format's width budget before committing.

(No verification or commit step on this task — it's a template reference.)

---

## Task 8: formats/flowchart.md

**Files:**
- Create: `formats/flowchart.md`

- [ ] **Step 1: Author the file using the Task 7 template with these values**

- **Purpose:** Visualize a mixed process that contains both sequential steps and branching decisions.
- **Use when:** mixed process with branches; >1 decision point in a flow; representing alternate paths; documenting code/system logic with conditionals
- **Do NOT use when:** pure yes/no branching to outcomes (use `decision-tree`); pure linear sequence (use `step-by-step-guide`); pure system topology (use `architecture-diagram`)
- **Width budget:** 100
- **Arc:** N/A (single artifact, no panel arc)
- **Layout rules:**
  - Top-to-bottom flow; primary path goes down
  - Decisions in diamonds (`◆` Unicode, `<>` ASCII), processes in rectangles
  - Branch labels (`Yes` / `No` / condition) on arrows leaving a decision
  - Loop-backs use a vertical line on the right margin returning up to the loop entry
- **Canonical skeleton — Unicode:**
  ```
  ┌──────────────┐
  │  Start       │
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │  Step 1      │
  └──────┬───────┘
         │
         ▼
       ◆ Decision ◆
      /           \
   Yes             No
    │               │
    ▼               ▼
  ┌──────────┐  ┌──────────┐
  │  Path A  │  │  Path B  │
  └────┬─────┘  └────┬─────┘
       │             │
       └──────┬──────┘
              ▼
        ┌──────────┐
        │  End     │
        └──────────┘
  ```
- **Canonical skeleton — ASCII fallback:**
  ```
  +--------------+
  |  Start       |
  +------+-------+
         |
         v
  +--------------+
  |  Step 1      |
  +------+-------+
         |
         v
       <Decision>
       /        \
    Yes          No
     |            |
     v            v
  +--------+  +--------+
  | Path A |  | Path B |
  +---+----+  +----+---+
      |            |
      +-----+------+
            v
        +-------+
        |  End  |
        +-------+
  ```
- **Short rendered example:** A "user signup flow" — Start → Submit form → Valid? → (yes) Create account → Send welcome email → End / (no) Show errors → loop back to Submit form
- **Failure modes:**
  - If decisions exceed 3 in a single flow → split into linked sub-flowcharts
  - If width overflows at 100 cols → shorten labels; if still failing, fall back to `step-by-step-guide` (collapse branches into dominant path)
  - If pure branching with no shared sequence → switch to `decision-tree`

- [ ] **Step 2: Verify**

Re-read. Confirm: all template sections present, both Unicode and ASCII skeletons close cleanly, example uses real topic and fits within 100 cols.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/flowchart.md && \
  git commit -m "feat: add flowchart format"
```

---

## Task 9: formats/slideshow.md

**Files:**
- Create: `formats/slideshow.md`

- [ ] **Step 1: Author the file using the Task 7 template with these values**

- **Purpose:** Multi-panel narrative explainer that walks the reader through a concept slide-by-slide.
- **Use when:** teaching a concept; "make me a slideshow on X"; turning a topic into shareable slides; explainer threads
- **Do NOT use when:** compressing into one image-like artifact (use `infographic`); SOP / repeatable workflow (use `playbook`); skill progression (use `learning-ladder`)
- **Width budget:** 80
- **Arc:** A1 — Slideshow arc (5–7 panels): hook → context → core idea → 2 examples → takeaway → optional recap
- **Layout rules:**
  - One panel per `## Slide N — <title>` heading
  - Each panel wrapped in a 78-col-wide framed box (leave 1 col padding on each side of the 80-col budget)
  - Panel titles centered or left-aligned consistently
  - Body inside box, left-aligned, single bullet style throughout
  - Slides separated by `\n\n---\n\n` in the saved `.md`
- **Canonical skeleton — Unicode:**
  ```
  ## Slide 1 — Hook

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                                                                              │
  │   <Hook sentence — short, vivid>                                             │
  │                                                                              │
  │   <One supporting line>                                                      │
  │                                                                              │
  └──────────────────────────────────────────────────────────────────────────────┘

  ---

  ## Slide 2 — Context

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │                                                                              │
  │   Why this matters:                                                          │
  │                                                                              │
  │   • <point 1>                                                                │
  │   • <point 2>                                                                │
  │   • <point 3>                                                                │
  │                                                                              │
  └──────────────────────────────────────────────────────────────────────────────┘
  ```
- **Canonical skeleton — ASCII fallback:** Same structure with `+`/`-`/`|` and `*` bullets.
- **Short rendered example:** A 5-panel slideshow on "Why ASCII diagrams beat screenshots for documentation" — Hook → Context → Core idea → 2 examples → Takeaway.
- **Failure modes:**
  - If content doesn't fill 5+ panels → reduce panel count, don't add filler (per U4)
  - If width overflows on a single panel → break body into 2 slides
  - If the topic doesn't have a narrative arc → fall back to `infographic`

- [ ] **Step 2: Verify** — Re-read, confirm template sections + arc reference + panel boxes are exactly 78 cols wide within the 80-col budget.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/slideshow.md && \
  git commit -m "feat: add slideshow format"
```

---

## Task 10: formats/playbook.md

**Files:**
- Create: `formats/playbook.md`

- [ ] **Step 1: Author the file using the Task 7 template with these values**

- **Purpose:** A repeatable workflow / SOP with clear objective, prerequisites, sequenced steps, success criteria, and failure modes.
- **Use when:** SOP / process you'll do more than once; AI agent system spec; marketing/ops execution; runbook
- **Do NOT use when:** simple linear walkthrough (use `step-by-step-guide`); teaching a concept (use `slideshow`); pure decision logic (use `decision-tree` or `flowchart`)
- **Width budget:** 80
- **Arc:** A2 — Playbook arc (5–8 panels): objective → when to use → prerequisites → steps → success criteria → failure modes
- **Layout rules:**
  - Each arc section is a `## Section` heading
  - Steps inside the "Execution" section get numbered (`### Step 1`, `### Step 2`...)
  - Each step has: action (one line), expected outcome (one line), check (one line)
  - Failure modes block is a 2-col table: `Symptom | Fix`
- **Canonical skeleton — Unicode:** (see file)
  ```
  ## Objective
  <one sentence>

  ## When to use
  - <context 1>
  - <context 2>

  ## Prerequisites
  - <prereq 1>
  - <prereq 2>

  ## Execution

  ### Step 1 — <verb-phrase title>
  Action:    <do this>
  Outcome:   <result>
  Check:     <how to verify>

  ### Step 2 — <verb-phrase title>
  Action:    <do this>
  Outcome:   <result>
  Check:     <how to verify>

  ## Success criteria
  ✓ <criterion 1>
  ✓ <criterion 2>

  ## Failure modes
  ┌────────────────────────────┬────────────────────────────────────────────────┐
  │ Symptom                    │ Fix                                            │
  ├────────────────────────────┼────────────────────────────────────────────────┤
  │ <symptom 1>                │ <fix 1>                                        │
  │ <symptom 2>                │ <fix 2>                                        │
  └────────────────────────────┴────────────────────────────────────────────────┘
  ```
- **Canonical skeleton — ASCII fallback:** `✓` → `[x]`, box-drawing → `+`/`-`/`|`.
- **Short rendered example:** A "cold outbound playbook" — Objective: book 10 demos/week. When to use: launching new offer. Prerequisites: ICP defined, sequence tool ready. Steps: build list → draft sequence → send → reply triage → book. Success criteria: 10 demos. Failure modes: low reply rate → tighten subject lines.
- **Failure modes:**
  - If steps < 3 → fall back to `step-by-step-guide`
  - If width overflows → shorten action/outcome/check phrasing; if still tight, split failure-modes table to its own section

- [ ] **Step 2: Verify** — Sections match A2 arc; failure-modes table fits in 80 cols.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/playbook.md && \
  git commit -m "feat: add playbook format"
```

---

## Task 11: formats/cheat-sheet.md

**Files:** Create `formats/cheat-sheet.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Dense reference card — maximal information density per square inch, for lookup not learning.
- **Use when:** quick reference; command/keyboard/syntax lookup; "give me a cheat sheet for X"
- **Do NOT use when:** narrative explanation needed (use `slideshow`); side-by-side options to choose between (use `comparison-table`)
- **Width budget:** 90–100 (target 95)
- **Arc:** N/A (single artifact, optionally multi-section)
- **Layout rules:**
  - Multi-column layout when content fits — 2 or 3 columns of equal width
  - Section headers in heavy borders to demarcate
  - One concept per line, `<term> · <definition>` style with `·` (Unicode) or `:` (ASCII)
  - No wasted vertical space; compact spacing
- **Canonical skeleton — Unicode:**
  ```
  ╔═══════════════════════════════════════════════════════════════════════════════╗
  ║                            <Cheat-sheet title>                                ║
  ╚═══════════════════════════════════════════════════════════════════════════════╝

  ┌─ <Section A> ──────────────────────┬─ <Section B> ──────────────────────────┐
  │ <term 1>   · <short definition>    │ <term 1>   · <short definition>        │
  │ <term 2>   · <short definition>    │ <term 2>   · <short definition>        │
  │ <term 3>   · <short definition>    │ <term 3>   · <short definition>        │
  └────────────────────────────────────┴────────────────────────────────────────┘

  ┌─ <Section C> ─────────────────────────────────────────────────────────────────┐
  │ <term 1>   · <short definition>                                              │
  │ <term 2>   · <short definition>                                              │
  └──────────────────────────────────────────────────────────────────────────────┘
  ```
- **Canonical skeleton — ASCII fallback:** Standard `+`/`-`/`|` boxes.
- **Short rendered example:** A "git cheat sheet" — sections: Stage/Commit, Branch, History, Remotes.
- **Failure modes:**
  - If content is sparse → switch to single-column
  - If too dense to read → split into multi-section cheat-sheet with explicit section headings (now auto-saves per save rules)

- [ ] **Step 2: Verify** — Multi-column structure aligns; widths sum to ≤95.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/cheat-sheet.md && \
  git commit -m "feat: add cheat-sheet format"
```

---

## Task 12: formats/step-by-step-guide.md

**Files:** Create `formats/step-by-step-guide.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Simple linear walkthrough — goal, steps, outcome. Lighter than `playbook` (no prerequisites/success criteria/failure modes block).
- **Use when:** simple sequential process; "walk me through X"; how-to for a one-off task
- **Do NOT use when:** SOP needing prerequisites + failure modes (use `playbook`); branching logic (use `flowchart`); pure decisions (use `decision-tree`)
- **Width budget:** 80
- **Arc:** A8 — Step-by-step-guide arc (4–7 steps): goal → steps → outcome
- **Layout rules:**
  - `## Goal` at top, single sentence
  - Numbered steps as `### Step N — <verb-phrase>`
  - Each step body: 1-3 lines max
  - `## Outcome` at end with verification line
- **Canonical skeleton — Unicode:**
  ```
  ## Goal
  <one-sentence goal>

  ### Step 1 — <verb-phrase>
  <what to do>

       ▼

  ### Step 2 — <verb-phrase>
  <what to do>

       ▼

  ### Step 3 — <verb-phrase>
  <what to do>

       ▼

  ## Outcome
  ✓ <what success looks like>
  ```
- **Canonical skeleton — ASCII fallback:** `▼` → `v`, `✓` → `[x]`.
- **Short rendered example:** "Set up Vercel project" — Goal: deploy a Next.js app. Steps: install CLI → link project → set env vars → deploy. Outcome: live preview URL.
- **Failure modes:**
  - If steps > 7 → switch to `playbook`
  - If steps branch → switch to `flowchart`

- [ ] **Step 2: Verify** — A8 arc reflected; ≤7 steps in skeleton.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/step-by-step-guide.md && \
  git commit -m "feat: add step-by-step-guide format"
```

---

## Checkpoint A — Mid-Phase 3 acceptance (after first 5 formats)

**Purpose:** Catch systemic template errors before propagating them across the remaining 11 format files.

**Do not skip.** If issues are found here, fix the format-file template + the affected files BEFORE continuing to Task 13.

- [ ] **Step 1: Run 5 mini-prompts against Claude with the partial skill installed (SKILL.md + composition-rules/ + the 5 format files authored so far)**

For each prompt, verify the structural expectations. The skill should auto-load the right format file and produce output that meets them.

| # | Prompt | Format expected | Structural expectations |
|---|---|---|---|
| A1 | "Make a flowchart for resetting a forgotten password." | flowchart | ≥1 decision node (`◆` or `<>`); Yes/No labels; ≤100 cols; single-artifact (no `---` dividers); NO file created |
| A2 | "Give me a 5-slide ASCII slideshow on debugging a slow SQL query." | slideshow | exactly 5 panels (user override); each panel framed in a ≤78-col box; ≤80 cols; slides separated by `---`; FILE created |
| A3 | "Playbook for onboarding a new contractor to our PPC agency." | playbook | sections: Objective, When to use, Prerequisites, Execution (4–6 steps with Action/Outcome/Check), Success criteria, Failure modes; failure-modes section is a 2-col table; ≤80 cols; FILE created |
| A4 | "Cheat sheet of common Tailwind utility classes." | cheat-sheet | multi-column layout (2 or 3 cols); section headers in heavy borders; ≤100 cols; one concept per line in `<term> · <def>` format |
| A5 | "Walk me through setting up GitHub Actions for a Node project, step by step." | step-by-step-guide | `## Goal` at top; 4–7 numbered steps; `## Outcome` at end with `✓` check; ≤80 cols |

- [ ] **Step 2: Document checkpoint results inline (do not create a separate file yet)**

For each of A1–A5, note in this plan file (under this checkpoint) whether structural expectations passed. If any failed, identify whether the cause is:
- The format file (specific fix needed in formats/<name>.md)
- The shared template (Task 7 — affects all 16 format files)
- A composition rule (Tasks 2–5)
- SKILL.md routing (Task 6)

- [ ] **Step 3: Fix any systemic issues BEFORE continuing**

If Task 7 (shared template) is wrong → fix it and re-author any of the 5 format files that depended on the broken template.

If a composition rule is wrong → fix it once; the remaining 11 format files will inherit the fix automatically.

If SKILL.md routing is wrong → fix Task 6 output and re-test only the prompts that depended on the routing.

If individual format files have problems → fix them, recommit with `fix: <description>`.

- [ ] **Step 4: Confirm all 5 mini-prompts pass before proceeding**

Only continue to Task 13 once A1–A5 all meet their structural expectations. This gate is non-negotiable — the remaining 11 format files will inherit any unfixed template/rule defect, multiplying rework.

- [ ] **Step 5: Commit the checkpoint results**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add docs/2026-05-17-ascii-canvas-implementation-plan.md && \
  git commit -m "docs: record Checkpoint A acceptance results"
```

---

## Task 13: formats/comparison-table.md

**Files:** Create `formats/comparison-table.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Side-by-side comparison of 2–5 options across a shared set of dimensions.
- **Use when:** evaluating choices; pros/cons across options; feature comparison; framework comparison
- **Do NOT use when:** narrative trade-off discussion needed (use `slideshow`); single option deep-dive (use `cheat-sheet`)
- **Width budget:** 90–100
- **Arc:** N/A
- **Layout rules:**
  - Header row: option names
  - Leftmost column: dimensions
  - Use ✓ / ✗ / ~ for quick categorical, prose for nuanced
  - Equal-width columns when option count is even
- **Canonical skeleton — Unicode:**
  ```
  ┌────────────────────┬─────────────────┬─────────────────┬─────────────────┐
  │ Dimension          │ Option A        │ Option B        │ Option C        │
  ├────────────────────┼─────────────────┼─────────────────┼─────────────────┤
  │ <dim 1>            │ <value>         │ <value>         │ <value>         │
  │ <dim 2>            │ ✓               │ ✗               │ ~               │
  │ <dim 3>            │ <value>         │ <value>         │ <value>         │
  └────────────────────┴─────────────────┴─────────────────┴─────────────────┘
  ```
- **Canonical skeleton — ASCII fallback:** `+`/`-`/`|`, `✓`/`✗`/`~` → `Y`/`N`/`~`.
- **Short rendered example:** "PostgreSQL vs MySQL vs SQLite across schema flexibility, replication, embedded use."
- **Failure modes:**
  - If >5 options → reduce dimensions or split into themed tables
  - If dimensions need narrative → fall back to `slideshow`

- [ ] **Step 2: Verify** — Columns equal width; total ≤100.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/comparison-table.md && \
  git commit -m "feat: add comparison-table format"
```

---

## Task 14: formats/timeline.md

**Files:** Create `formats/timeline.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Show events sequenced on a horizontal or vertical axis.
- **Use when:** history; product roadmap; project schedule; sequence of past events
- **Do NOT use when:** events have decision branches (use `flowchart`); concept dependencies (use `concept-stack`)
- **Width budget:** 100
- **Arc:** N/A
- **Layout rules:**
  - Vertical timeline preferred for >4 events (each gets its own line set)
  - Horizontal timeline for ≤4 short labels
  - Each event has: date (or step label) + title + 1-line description
  - Anchor markers (`●`) on the axis at each event
- **Canonical skeleton — Unicode (vertical):**
  ```
  ●  <Date 1>      <Event title 1>
  │                <One-line description>
  │
  ●  <Date 2>      <Event title 2>
  │                <One-line description>
  │
  ●  <Date 3>      <Event title 3>
  │                <One-line description>
  ▼
  ```
- **Canonical skeleton — Unicode (horizontal):**
  ```
  ●─────────●─────────●─────────●
  Date 1    Date 2    Date 3    Date 4
  Event A   Event B   Event C   Event D
  ```
- **Canonical skeleton — ASCII fallback:** `●` → `*`, `│` → `|`, `─` → `-`, `▼` → `v`.
- **Short rendered example:** "SponsorCal product milestones 2025-2026."
- **Failure modes:**
  - If events > 12 → split into era-based timelines
  - If timeline has branching forks → switch to `flowchart`

- [ ] **Step 2: Verify** — Both axis orientations present; horizontal version ≤100 cols.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/timeline.md && \
  git commit -m "feat: add timeline format"
```

---

## Task 15: formats/decision-tree.md

**Files:** Create `formats/decision-tree.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Strict yes/no branching to outcomes. Each node is a question; each leaf is an outcome.
- **Use when:** classification / decision logic; "should I X or Y?"; troubleshooting; routing logic
- **Do NOT use when:** mixed steps and branches (use `flowchart`); >3 branches per node (use `flowchart`)
- **Width budget:** 100
- **Arc:** N/A
- **Layout rules:**
  - Root at top
  - Yes/No labels on every branch
  - Outcomes (leaves) in distinct visual style (e.g. heavy border or `[Outcome]` wrapper)
  - Tree depth ≤ 4 (deeper → split into linked trees)
- **Canonical skeleton — Unicode:**
  ```
            ◆ Question 1? ◆
           /              \
        Yes                No
         │                  │
         ▼                  ▼
   ◆ Question 2? ◆    [Outcome X]
       /     \
     Yes      No
      │        │
      ▼        ▼
  [Outcome A] [Outcome B]
  ```
- **Canonical skeleton — ASCII fallback:** `◆` → `<>`, `▼` → `v`, `─` → `-`.
- **Short rendered example:** "Should I use Server Actions or API Routes in Next.js?"
- **Failure modes:**
  - If tree depth > 4 → split into chained trees with named entry points
  - If branches > 2 per node → switch to `flowchart`

- [ ] **Step 2: Verify** — Yes/No labels on every branch; max depth ≤ 4.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/decision-tree.md && \
  git commit -m "feat: add decision-tree format"
```

---

## Task 16: formats/architecture-diagram.md

**Files:** Create `formats/architecture-diagram.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Visualize system components, services, and how they connect.
- **Use when:** showing service topology; data flow between components; layered systems; deployment architecture
- **Do NOT use when:** time-ordered interactions (use `sequence-diagram`); pure layering (use `concept-stack`)
- **Width budget:** 110–120
- **Arc:** N/A
- **Layout rules:**
  - Boxes for services/components; lines for connections; labels on connections describing protocol/payload
  - Layered top-to-bottom or left-to-right by tier (frontend → backend → data)
  - Group related components in shared region (with light border or shaded box)
  - External systems use double border (`╔═╗`); internal use single (`┌─┐`)
- **Canonical skeleton — Unicode:**
  ```
  ╔═══════════════════════════════════════════════════════════════════════════════════════╗
  ║                              Client (Browser)                                         ║
  ╚════════════════════════════════════┬══════════════════════════════════════════════════╝
                                       │ HTTPS
                                       ▼
  ┌───────────────────────────────────────────────────────────────────────────────────────┐
  │                              Edge / CDN                                               │
  └───────────────────┬────────────────────────────────┬──────────────────────────────────┘
                      │ static                          │ dynamic
                      ▼                                  ▼
              ┌──────────────┐                  ┌──────────────────┐
              │   Bucket     │                  │   API Server     │
              └──────────────┘                  └────────┬─────────┘
                                                          │ SQL
                                                          ▼
                                                 ╔══════════════════╗
                                                 ║   PostgreSQL     ║
                                                 ╚══════════════════╝
  ```
- **Canonical skeleton — ASCII fallback:** `╔═╗` → `+=+`, `┌─┐` → `+-+`, `▼` → `v`.
- **Short rendered example:** "SponsorCal architecture: client → Vercel edge → Next.js API → Prisma → Neon Postgres + Vercel Blob."
- **Failure modes:**
  - If components > 8 → group into sub-diagrams
  - If too tall → switch to left-to-right orientation

- [ ] **Step 2: Verify** — External components use double border, internal use single; width ≤120.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/architecture-diagram.md && \
  git commit -m "feat: add architecture-diagram format"
```

---

## Task 17: formats/sequence-diagram.md

**Files:** Create `formats/sequence-diagram.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Show time-ordered messages between named actors.
- **Use when:** API call flows; auth handshakes; protocol interactions; "what happens when user clicks X"
- **Do NOT use when:** static topology (use `architecture-diagram`); no time ordering (use `flowchart`)
- **Width budget:** 110–120
- **Arc:** N/A
- **Layout rules:**
  - Actor columns at top, each as a `[Actor]` box with a vertical lifeline below
  - Messages flow as horizontal arrows between lifelines, time descending
  - Each arrow labeled with the message name
  - Activation boxes (`▒`) optional for showing actor activity windows
- **Canonical skeleton — Unicode:**
  ```
   [Client]               [API]                 [DB]
      │                    │                    │
      │── POST /login ───▶ │                    │
      │                    │── SELECT user ───▶ │
      │                    │                    │
      │                    │◀── user record ────│
      │                    │                    │
      │◀── 200 + token ────│                    │
      │                    │                    │
  ```
- **Canonical skeleton — ASCII fallback:** `─▶` → `-->`, `◀─` → `<--`, `│` → `|`.
- **Short rendered example:** "Stripe checkout flow — Client → SponsorCal API → Stripe → Webhook → SponsorCal API → DB."
- **Failure modes:**
  - If actors > 4 → split into sub-flows
  - If messages > 12 → split into pre/post phases

- [ ] **Step 2: Verify** — Lifelines align; arrows directional; width ≤120.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/sequence-diagram.md && \
  git commit -m "feat: add sequence-diagram format"
```

---

## Task 18: formats/mind-map.md

**Files:** Create `formats/mind-map.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Central idea with radial branches of related concepts.
- **Use when:** brainstorming; exploring a concept's facets; ideation around a topic
- **Do NOT use when:** content packaging across formats (use `content-map`); strict hierarchy (use `concept-stack`)
- **Width budget:** 100
- **Arc:** N/A
- **Layout rules:**
  - Central node in the middle (or top-left if vertical)
  - Branches radiate out — represented as indented sub-trees with leader lines
  - Depth ≤ 3 (root → branch → leaf)
  - Each leaf is one concept
- **Canonical skeleton — Unicode:**
  ```
                    ┌──────────────┐
                    │  Core Idea   │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │ Branch A│        │ Branch B│        │ Branch C│
   └────┬────┘        └────┬────┘        └────┬────┘
        │                  │                  │
     • leaf A1          • leaf B1          • leaf C1
     • leaf A2          • leaf B2          • leaf C2
     • leaf A3                             • leaf C3
  ```
- **Canonical skeleton — ASCII fallback:** Box-drawing → `+`/`-`/`|`; `•` → `*`.
- **Short rendered example:** "ASCII diagrams" — branches: formats, use cases, tools, taxonomy.
- **Failure modes:**
  - If depth > 3 → switch to `concept-stack` or `content-map`
  - If too wide → reorient vertical with indented branches

- [ ] **Step 2: Verify** — Depth ≤3; total width ≤100.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/mind-map.md && \
  git commit -m "feat: add mind-map format"
```

---

## Task 19: formats/dashboard.md

**Files:** Create `formats/dashboard.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Status board showing key metrics/sections at a glance.
- **Use when:** weekly status; KPI summary; ops board; "give me a dashboard for X"
- **Do NOT use when:** narrative explanation (use `slideshow`); single-purpose reference (use `cheat-sheet`)
- **Width budget:** 120
- **Arc:** A7 — Dashboard arc (4–8 panels): title → metrics → summary
- **Layout rules:**
  - Top banner: title + as-of date
  - 2x2 or 3x2 grid of metric blocks
  - Each block: name, value (big), trend indicator (`▲ ▼ ▬`), one supporting line
  - Bottom summary band: one-line signal
- **Canonical skeleton — Unicode:**
  ```
  ╔════════════════════════════════════════════════════════════════════════════════════════════════════════╗
  ║   <Dashboard title>                                                           as of <YYYY-MM-DD>       ║
  ╚════════════════════════════════════════════════════════════════════════════════════════════════════════╝

  ┌──────────────────────────────┬──────────────────────────────┬──────────────────────────────────────────┐
  │  <Metric A>                  │  <Metric B>                  │  <Metric C>                              │
  │                              │                              │                                          │
  │     <BIG VALUE>  ▲           │     <BIG VALUE>  ▼           │     <BIG VALUE>  ▬                       │
  │                              │                              │                                          │
  │  <one supporting line>       │  <one supporting line>       │  <one supporting line>                   │
  └──────────────────────────────┴──────────────────────────────┴──────────────────────────────────────────┘
  ┌──────────────────────────────┬──────────────────────────────┬──────────────────────────────────────────┐
  │  <Metric D>                  │  <Metric E>                  │  <Metric F>                              │
  │     <BIG VALUE>  ▲           │     <BIG VALUE>  ▲           │     <BIG VALUE>  ▼                       │
  │  <one supporting line>       │  <one supporting line>       │  <one supporting line>                   │
  └──────────────────────────────┴──────────────────────────────┴──────────────────────────────────────────┘

  Summary: <one-line signal — what's the headline read?>
  ```
- **Canonical skeleton — ASCII fallback:** Box-drawing → `+`/`=`/`|`; `▲▼▬` → `^v-`.
- **Short rendered example:** "SponsorCal weekly metrics — bookings, MRR, churn, active creators."
- **Failure modes:**
  - If metrics > 8 → split into themed dashboards
  - If supporting lines need detail → fall back to `cheat-sheet`

- [ ] **Step 2: Verify** — Grid aligns; widths sum to ≤120.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/dashboard.md && \
  git commit -m "feat: add dashboard format"
```

---

## Task 20: formats/learning-ladder.md

**Files:** Create `formats/learning-ladder.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Show a skill or topic progressing from beginner to expert across discrete rungs.
- **Use when:** "make a learning path for X"; skill-progression maps; curriculum design; education content
- **Do NOT use when:** linear how-to (use `step-by-step-guide`); concept-not-skill (use `concept-stack`)
- **Width budget:** 80
- **Arc:** A3 — Learning-ladder arc (4–6 levels): foundations → mechanics → application → intermediate → advanced → expert
- **Layout rules:**
  - Vertical ladder with rungs from bottom (foundations) to top (expert)
  - Each rung: level name + 2-3 sub-skills
  - Use `▲` between rungs to indicate progression
- **Canonical skeleton — Unicode:**
  ```
  ┌─ Level 6: Expert ───────────────────────────────────────────────────────────┐
  │  • <expert skill 1>                                                         │
  │  • <expert skill 2>                                                         │
  └─────────────────────────────────────────────────────────────────────────────┘
                                        ▲
  ┌─ Level 5: Advanced ─────────────────────────────────────────────────────────┐
  │  • <advanced skill 1>                                                       │
  │  • <advanced skill 2>                                                       │
  └─────────────────────────────────────────────────────────────────────────────┘
                                        ▲
  ┌─ Level 4: Intermediate ─────────────────────────────────────────────────────┐
  │  • <intermediate skill 1>                                                   │
  └─────────────────────────────────────────────────────────────────────────────┘
                                        ▲
  ┌─ Level 3: Application ──────────────────────────────────────────────────────┐
  │  • <practical skill 1>                                                      │
  └─────────────────────────────────────────────────────────────────────────────┘
                                        ▲
  ┌─ Level 2: Core mechanics ───────────────────────────────────────────────────┐
  │  • <mechanic 1>                                                             │
  │  • <mechanic 2>                                                             │
  └─────────────────────────────────────────────────────────────────────────────┘
                                        ▲
  ┌─ Level 1: Foundations ──────────────────────────────────────────────────────┐
  │  • <foundation 1>                                                           │
  │  • <foundation 2>                                                           │
  └─────────────────────────────────────────────────────────────────────────────┘
  ```
- **Canonical skeleton — ASCII fallback:** Box-drawing → `+`/`-`/`|`, `▲` → `^`, `•` → `*`.
- **Short rendered example:** "Spanish verb conjugation ladder — present tense → preterite/imperfect → subjunctive → conditionals → fluency drills."
- **Failure modes:**
  - If levels < 4 → fall back to `step-by-step-guide`
  - If skills are non-progressive → switch to `concept-stack` or `mind-map`

- [ ] **Step 2: Verify** — 4-6 rungs; A3 arc respected; widths ≤80.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/learning-ladder.md && \
  git commit -m "feat: add learning-ladder format"
```

---

## Task 21: formats/infographic.md

**Files:** Create `formats/infographic.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Compress a topic into a single visual artifact with a headline + supporting blocks + synthesis.
- **Use when:** "turn this into an infographic"; high-signal summary; shareable social-style asset
- **Do NOT use when:** narrative needed (use `slideshow`); reference lookup (use `cheat-sheet`)
- **Width budget:** 100
- **Arc:** A4 — Infographic arc (4–6 blocks): headline → 3 supporting insights → synthesis → optional CTA
- **Layout rules:**
  - Headline at top in heavy border
  - 3 supporting blocks in a horizontal row (3-col grid) — each with mini-icon + stat + label
  - Synthesis block at bottom
  - Optional CTA in centered footer
- **Canonical skeleton — Unicode:**
  ```
  ╔══════════════════════════════════════════════════════════════════════════════════════════════════╗
  ║                              <HEADLINE / KEY CLAIM>                                             ║
  ╚══════════════════════════════════════════════════════════════════════════════════════════════════╝

  ┌────────────────────────────┬────────────────────────────┬─────────────────────────────────────────┐
  │      ◆                     │      ▲                     │      ●                                  │
  │   <BIG STAT>               │   <BIG STAT>               │   <BIG STAT>                            │
  │   <label>                  │   <label>                  │   <label>                               │
  └────────────────────────────┴────────────────────────────┴─────────────────────────────────────────┘

  ┌─ Synthesis ────────────────────────────────────────────────────────────────────────────────────────┐
  │  <The single takeaway in 1-2 lines>                                                               │
  └────────────────────────────────────────────────────────────────────────────────────────────────────┘

                                          → <Optional CTA / next step>
  ```
- **Canonical skeleton — ASCII fallback:** Box-drawing → `+`/`=`/`|`; `◆▲●` → `*`/`^`/`o`; `→` → `->`.
- **Short rendered example:** "Why ASCII beats screenshots — 3 stats: portable, searchable, editable."
- **Failure modes:**
  - If supporting blocks > 4 → split into 2 rows
  - If headline + stats + synthesis don't tell a complete story → fall back to `slideshow`

- [ ] **Step 2: Verify** — Width ≤100; 3-col grid aligns; A4 arc respected.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/infographic.md && \
  git commit -m "feat: add infographic format"
```

---

## Task 22: formats/content-map.md

**Files:** Create `formats/content-map.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Take one core idea and expand it into derived content artifacts — hooks, posts, threads, angles.
- **Use when:** social content planning; one idea → many posts; X/LinkedIn content strategy
- **Do NOT use when:** ideation/brainstorming (use `mind-map`); concept dependency (use `concept-stack`)
- **Width budget:** 100
- **Arc:** A6 — Content-map arc (3–5 clusters): core idea → angle clusters → optional distribution
- **Layout rules:**
  - Core idea at top in heavy border
  - Below: 3–5 angle clusters, each as a labeled column or row group
  - Each cluster contains 2–4 specific content units (hooks, post titles, thread topics)
  - Optional distribution notes at bottom (channels, cadence)
- **Canonical skeleton — Unicode:**
  ```
  ╔══════════════════════════════════════════════════════════════════════════════════════════════════╗
  ║                                <CORE IDEA>                                                        ║
  ╚══════════════════════════════════════════════════════════════════════════════════════════════════╝
                                              │
              ┌───────────────────────────────┼───────────────────────────────┐
              │                               │                               │
   ┌─ Angle: <theme A> ─┐         ┌─ Angle: <theme B> ─┐         ┌─ Angle: <theme C> ─┐
   │  ▸ Hook: <…>       │         │  ▸ Hook: <…>       │         │  ▸ Hook: <…>       │
   │  ▸ Post: <…>       │         │  ▸ Post: <…>       │         │  ▸ Post: <…>       │
   │  ▸ Thread: <…>     │         │  ▸ Thread: <…>     │         │  ▸ Thread: <…>     │
   └────────────────────┘         └────────────────────┘         └────────────────────┘

  Distribution: <channels and cadence — optional>
  ```
- **Canonical skeleton — ASCII fallback:** Box-drawing → `+`/`=`/`|`; `▸` → `>`.
- **Short rendered example:** "Core idea: ASCII diagrams beat screenshots. Angles: portability, searchability, editability."
- **Failure modes:**
  - If clusters > 5 → group or prioritize
  - If each cluster has 1 content unit → fall back to `mind-map`

- [ ] **Step 2: Verify** — Width ≤100; clusters align; A6 arc respected.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/content-map.md && \
  git commit -m "feat: add content-map format"
```

---

## Task 23: formats/concept-stack.md

**Files:** Create `formats/concept-stack.md`

- [ ] **Step 1: Author per Task 7 template with these values**

- **Purpose:** Show layers / dependencies where each layer rests on the one below.
- **Use when:** "show me how X builds on Y"; tech stack; abstraction layers; systems-thinking layouts
- **Do NOT use when:** system topology with cross-connections (use `architecture-diagram`); idea radiating outward (use `mind-map`)
- **Width budget:** 100
- **Arc:** A5 — Concept-stack arc (4–6 layers): base → supporting layers → top outcome
- **Layout rules:**
  - Layers stacked vertically, widest at bottom (foundation), narrowest at top (outcome) OR uniform width
  - Each layer labeled with name + 1-line role
  - Edges between layers carry implicit "depends on" semantics — no labels needed
- **Canonical skeleton — Unicode:**
  ```
              ┌──────────────────────────────────────────────┐
              │           Top: <Outcome / Goal>              │
              └──────────────────────────────────────────────┘
            ┌──────────────────────────────────────────────────┐
            │           Layer 4: <name + role>                │
            └──────────────────────────────────────────────────┘
          ┌──────────────────────────────────────────────────────┐
          │           Layer 3: <name + role>                    │
          └──────────────────────────────────────────────────────┘
        ┌──────────────────────────────────────────────────────────┐
        │           Layer 2: <name + role>                        │
        └──────────────────────────────────────────────────────────┘
      ┌──────────────────────────────────────────────────────────────┐
      │           Layer 1: <Foundation>                              │
      └──────────────────────────────────────────────────────────────┘
  ```
- **Canonical skeleton — ASCII fallback:** Box-drawing → `+`/`-`/`|`.
- **Short rendered example:** "Modern web app stack — DB → ORM → API → SSR framework → CDN → User."
- **Failure modes:**
  - If layers > 6 → group adjacent layers
  - If lateral connections needed → switch to `architecture-diagram`

- [ ] **Step 2: Verify** — 4-6 layers; A5 arc respected; widths ≤100.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add formats/concept-stack.md && \
  git commit -m "feat: add concept-stack format"
```

---

# Phase 4 — Example files (16, richer references)

## Task 24: Author the example-file template (reference for Tasks 25-40)

This task creates no file. It defines the shape every example file follows. Apply it to Tasks 25–40.

**Every example file (`examples/<format>-example.md`) MUST contain:**

````markdown
# <format> — full example

**Topic:** <real, specific topic>
**Width budget:** <N> cols
**Mode:** Unicode (default)

---

<the full rendered artifact, passing strict QA at the format's max width>

---

## Why this works

- <2-3 bullets explaining the layout choices: panel count, why this format vs alternatives, what the QA caught>

## ASCII variant (if structurally different from Unicode)

<only include if the ASCII layout differs from Unicode>
````

The example artifact must be **longer and richer** than the inline example in the format file — this is the deep reference loaded on demand when the inline example isn't enough.

(No verification or commit step on this task — it's a template reference.)

---

## Tasks 25–40: One example per format

For each of the 16 formats, create a richer example using the Task 24 template. Suggested topics (specific and real, not generic):

| Task | File | Topic |
|---|---|---|
| 25 | `examples/flowchart-example.md` | User signup with email verification + magic link fallback |
| 26 | `examples/slideshow-example.md` | 7-panel: "Why ASCII diagrams beat screenshots for docs" |
| 27 | `examples/playbook-example.md` | Cold outbound playbook — 6-step execution + 4 failure modes |
| 28 | `examples/cheat-sheet-example.md` | Git daily commands, multi-section (stage, branch, history, remote) |
| 29 | `examples/step-by-step-guide-example.md` | Set up a new Next.js project on Vercel |
| 30 | `examples/comparison-table-example.md` | PostgreSQL vs MySQL vs SQLite (6 dimensions) |
| 31 | `examples/timeline-example.md` | SponsorCal product milestones 2025-2026 |
| 32 | `examples/decision-tree-example.md` | "Should I use Server Actions or API Routes in Next.js?" |
| 33 | `examples/architecture-diagram-example.md` | SponsorCal architecture (client → edge → API → DB + Blob) |
| 34 | `examples/sequence-diagram-example.md` | SponsorCal Stripe Connect onboarding handshake (creator → SponsorCal API → Stripe → webhook → DB) |
| 35 | `examples/mind-map-example.md` | "ASCII diagrams" — branches: formats, use cases, tools, taxonomy |
| 36 | `examples/dashboard-example.md` | SponsorCal weekly metrics — bookings, MRR, churn, active creators |
| 37 | `examples/learning-ladder-example.md` | Spanish verb conjugation (6 levels) |
| 38 | `examples/infographic-example.md` | "Why ASCII beats screenshots" — 3 stats + synthesis + CTA |
| 39 | `examples/content-map-example.md` | "PPC agent skills" → 4 angle clusters for X/LinkedIn (hooks/posts/threads each) |
| 40 | `examples/concept-stack-example.md` | Modern web app stack (DB → ORM → API → SSR → CDN → user) |

**For each task (25–40):**

- [ ] **Step 1: Author the file** per the Task 24 template, using the topic from the table above. The full rendered artifact must pass strict QA at the format's max width.

- [ ] **Step 2: Verify** — Re-read. Confirm: full artifact present, "Why this works" bullets present, ASCII variant included only if structurally different.

- [ ] **Step 3: Commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add examples/<format>-example.md && \
  git commit -m "feat: add <format> example artifact"
```

Replace `<format>` with the format name in the file path and commit message.

---

# Phase 5 — End-to-end acceptance suite

## Task 41: Run the 7-prompt acceptance suite + fix any failures

This is the actual verification step. The skill is built; now confirm it behaves correctly end-to-end.

**Files:**
- Create: `docs/acceptance-suite-results.md`

- [ ] **Step 1: Run each of the 7 acceptance prompts against Claude with the ascii-canvas skill installed**

For each prompt below, in a fresh Claude conversation with the skill installed, paste the prompt and capture the output. The expected behavior column is what to verify.

| # | Prompt | Expected behavior | Structural expectations (must all hold) |
|---|---|---|---|
| 1 | "Make me a flowchart of how user signup works in a typical web app." | Skill fires. Loads `formats/flowchart.md`. Renders a single-artifact flowchart at ≤100 cols, Unicode. Does NOT save a file (single-artifact). Light QA. | (a) ≥1 decision node (`◆` or `<>`); (b) Yes/No (or condition) labels on every branch out of a decision; (c) max line width ≤ 100; (d) no `---` section dividers (single artifact); (e) NO file created in `~/Ascii-diagrams/` |
| 2 | "Break down how Spanish verb conjugation works for a learner — visually." | Skill fires (inferred multi-panel). Routes to `learning-ladder`. Loads `formats/learning-ladder.md` + `narrative-arcs.md` A3. Renders 4–6 rungs at ≤80 cols. Auto-saves to `~/Ascii-diagrams/2026-05/<slug>.md`. Strict QA. Emits exact 2-line post-save block. | (a) 4–6 distinct levels labeled Level 1..N; (b) each level boxed; (c) `▲` (or `^`) between rungs showing progression; (d) max line width ≤ 80; (e) file created at `~/Ascii-diagrams/2026-05/2026-05-17-<slug>.md`; (f) post-save line is EXACTLY 2 lines (`Saved to:` + path) |
| 3 | "Give me a playbook for cold outbound — keep it useful, not generic." | Skill fires. Routes to `playbook`. Loads `formats/playbook.md` + A2 arc. Renders objective → when → prerequisites → 4-6 steps → success → failure-modes table. Auto-saves. Strict QA. | (a) sections: Objective, When to use, Prerequisites, Execution, Success criteria, Failure modes (all present, in this order); (b) 4–6 numbered steps under Execution; (c) each step has Action/Outcome/Check lines; (d) Failure modes is a 2-column table; (e) max line width ≤ 80; (f) file created |
| 4 | "Draw the architecture of a Vercel-hosted Next.js app with Neon Postgres. Use plain ASCII — I'm pasting this into an email." | Skill fires. Routes to `architecture-diagram`. Detects "plain ASCII" → uses ASCII fallback per `box-drawing.md` R1. Width ≤120. | (a) ZERO Unicode box-drawing chars in output (no `─│┌┐└┘├┤┬┴┼╔═╗║◆▲●→←↓↑`); (b) only ASCII (`+ - \| < > * v ^`); (c) ≥3 named components; (d) connection lines labeled with protocol/payload; (e) max line width ≤ 120 |
| 5 | "Make me a 6-slide ASCII slideshow on why ASCII diagrams beat screenshots." | Skill fires. Routes to `slideshow`. User specified "6 slides" → A1 arc with N=6 (user override per U5). Auto-saves. Width 80. | (a) EXACTLY 6 panels (user override respected); (b) each panel has `## Slide N — <title>` heading; (c) each panel body framed in a ≤78-col box; (d) panels separated by `\n\n---\n\n`; (e) max line width ≤ 80; (f) file created |
| 6 | "What year did Vercel acquire next.js?" | Skill does NOT fire (simple factual question — explicit non-trigger). Plain prose response only. | (a) NO box-drawing characters in output; (b) NO `## Slide` / `## Section` headings; (c) response is ≤2 sentences of prose; (d) NO file created |
| 7 | "Make a dashboard of SponsorCal weekly metrics — bookings, MRR, churn, active creators, top creator, top sponsor, refund rate, NPS." (8 metrics → forces tight 120-col budget) | Skill fires. Routes to `dashboard`. Loads A7 arc. Renders 2x4 (or 4x2) grid at 120 cols. Strict QA catches any overflow. If overflow → fail-twice → simplify or fall back to `cheat-sheet`. Auto-saves. | (a) title banner at top with as-of date; (b) 8 metric blocks present (possibly fallback to cheat-sheet if dashboard cannot fit); (c) each block has name + value + trend indicator + supporting line (or fallback equivalent); (d) summary line at bottom; (e) max line width ≤ 120; (f) if fallback fires, note `Note: simplified from dashboard to <fallback>` appears; (g) file created |

**Pass rule:** A prompt passes ONLY if every structural expectation in column 4 holds. "Looks good" without checking each expectation is not a pass.

- [ ] **Step 2: Document the results**

Write `docs/acceptance-suite-results.md` with one section per prompt:

````markdown
# Acceptance suite — 2026-05-17

## Prompt 1: <prompt text>

**Expected:** <from table>
**Observed:** <what actually happened>
**Pass/Fail:** <verdict>
**Notes:** <any issues, regressions, or surprises>

[Repeat for prompts 2–7]

## Summary

- Pass: N/7
- Fail: N/7
- Issues to address: [list]
````

- [ ] **Step 3: Fix any failures**

For each failure:
- Identify which file/rule caused the wrong behavior
- Edit that file
- Re-run the failing prompt
- Re-verify
- Commit the fix with `fix: <description>`

Iterate until all 7 prompts pass.

- [ ] **Step 4: Final commit**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git add docs/acceptance-suite-results.md && \
  git commit -m "docs: record passing acceptance suite results"
```

- [ ] **Step 5: Tag v1**

```bash
cd ~/.claude/skills/ascii-canvas && \
  git tag -a v1.0.0 -m "ascii-canvas v1 — 16 formats, 8 arcs, strict QA, auto-save"
```

---

# Phase 6 — Install location

The skill lives at `~/.claude/skills/ascii-canvas/` — that's the canonical location Claude Code reads from. No symlink, no separate working directory: the editable source IS the install.

To install on another machine, clone the repo into that path:

```bash
git clone <repo-url> ~/.claude/skills/ascii-canvas
```

This is a one-time setup. Once linked, the skill description (in `SKILL.md` frontmatter) will fire it per the trigger rules.

---

# Done criteria

The skill is v1-complete when:

1. All 38 files exist on disk, committed to the `ascii-canvas` git repo
2. `git log --oneline` shows roughly 38 commits (one per file authored, plus fixups)
3. All 7 acceptance prompts pass without manual intervention
4. The skill correctly refuses to activate on simple factual queries
5. Auto-save writes to `~/Ascii-diagrams/YYYY-MM/YYYY-MM-DD-<slug>.md` with valid slugs
6. Strict QA catches a deliberate width overflow (manual test: ask for an over-wide dashboard, verify simplify or fallback fires)
7. Repository is tagged `v1.0.0`
